# Lemlist API key (from Settings > Integrations in lemlist)
LEMLIST_API_KEY=

# Vercel KV / Upstash Redis — auto-provisioned when you link a Vercel KV store.
# Local dev can leave these blank; cache transparently falls through to direct fetch.
KV_REST_API_URL=
KV_REST_API_TOKEN=
